# Função lambda que verifica se um número é par ou ímpar
# Usa if/else em uma única expressão
par_impar = lambda x: "Par" if x % 2 == 0 else "Impar"

# Chamada da função lambda
print(par_impar(2))
